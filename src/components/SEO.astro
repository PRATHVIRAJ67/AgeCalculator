---
interface Props {
  title?: string;
  description?: string;
  canonicalUrl?: string;
  type?: 'website' | 'article';
  image?: string;
  schema?: Record<string, any>;
}

const {
  title = 'Age Calculator | Accurate Age & Birthday Calculations',
  description = 'Calculate your exact age in years, months, days, hours, and seconds. Discover fun facts about your birthday, next milestone, and more.',
  canonicalUrl = Astro.site ? new URL(Astro.url.pathname, Astro.site).href : Astro.url.href,
  type = 'website',
  image = '/og-image.jpg', // Placeholder
  schema
} = Astro.props;

// Default Organization Schema
const defaultSchema = {
  "@context": "https://schema.org",
  "@type": "WebApplication",
  "name": "Age Calculator",
  "url": Astro.site?.toString() || "https://example.com",
  "applicationCategory": "CalculatorApplication",
  "operatingSystem": "All"
};

const finalSchema = schema || defaultSchema;
---

<!-- Primary Meta Tags -->
<title>{title}</title>
<meta name="title" content={title} />
<meta name="description" content={description} />
<link rel="canonical" href={canonicalUrl} />

<!-- Open Graph / Facebook -->
<meta property="og:type" content={type} />
<meta property="og:url" content={canonicalUrl} />
<meta property="og:title" content={title} />
<meta property="og:description" content={description} />
<meta property="og:image" content={new URL(image, Astro.site || 'https://example.com')} />

<!-- Twitter -->
<meta property="twitter:card" content="summary_large_image" />
<meta property="twitter:url" content={canonicalUrl} />
<meta property="twitter:title" content={title} />
<meta property="twitter:description" content={description} />
<meta property="twitter:image" content={new URL(image, Astro.site || 'https://example.com')} />

<!-- JSON-LD Schema -->
<script type="application/ld+json" set:html={JSON.stringify(finalSchema)} />
